# Tests + developer tooling (use a normal dev venv, e.g. .venv — not the PyInstaller build venv).
pytest>=8.0.0
pytest-qt>=4.4.0
pytest-mock>=3.12.0

# PyInstaller: also in requirements-build.txt for the dedicated build venv (.venv-build).
# Install pyinstaller here too if you use one venv for both dev and local one-off packaging.
pyinstaller>=6.6.0

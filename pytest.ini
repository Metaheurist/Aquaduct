[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    qt: desktop UI tests (PyQt6 + pytest-qt where used)
    slow: slow or integration-heavy tests (optional tier)
    cuda_gpu: requires real CUDA hardware (optional)
